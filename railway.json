{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "composer install --no-dev --optimize-autoloader --no-scripts && mkdir -p config/jwt && php bin/console lexik:jwt:generate-keypair --skip-if-exists"
  },
  "deploy": {
    "startCommand": "php -d variables_order=EGPCS -S 0.0.0.0:$PORT -t public/",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
